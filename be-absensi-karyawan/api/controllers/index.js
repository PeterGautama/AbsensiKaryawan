const employeeController = require("./employee.controller");
const authController = require("./auth.controller");
const absentController = require("./absent.controller");
const reportController = require("./report.controller");

module.exports = {
  employeeController,
  authController,
  absentController,
  reportController,
};
